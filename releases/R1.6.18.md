# MediaDock R1.6.18 — Development Candidate

Status: **development / not stable**

R1.6.18 preserves the strict 5 Video + 5 MP3 unlicensed trial and adds a fail-closed stable updater client.

Candidate Windows artifacts were built and staged-smoke verified:

- staged EXE SHA-256: $exeSha
- installer SHA-256: $installerSha
- portable ZIP SHA-256: $portableSha

The updater reads only latest-stable.json and requires the stable gate, exact MediaDock identity, a newer version, an installer hosted under this repository's GitHub Releases path, and a matching SHA-256.

The stable pointer remains inactive. The installer is not yet published here.

Next gate: install the R1.6.18 trial candidate, verify trial/update persistence, then publish the exact installer asset and move the stable pointer last.